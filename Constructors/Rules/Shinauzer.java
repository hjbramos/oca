public class Shinauzer extends Dog {
    // A call to super() can either be a nor-arg call or can include arguments passed to the super constructor.
    Shinauzer(String name) {
        super(name);
    }
}